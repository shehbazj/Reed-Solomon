//#include<iostream>
#include"galois.h"
#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main(int argc, char *argv[])
{

/*
  unsigned int x, w;

  if (argc != 3) {
    fprintf(stderr, "usage: gf_log x w - returns the discrete log if x in GF(2^w)\n");
    return 1;
  }

  sscanf(argv[1], "%u", &x);
  w = atoi(argv[2]);

  if (w < 1 || w > 32) { fprintf(stderr, "Bad w\n"); exit(1); }

  if (x == 0 || (w < 32 && x >= (1 << w))) { fprintf(stderr, "x must be in [1,%d]\n", (1 << w)-1); exit(1); }

  printf("%u\n", galois_log(x, w));
//  exit(0);
*/
/*
	if(argc != 5){
		std::cout << "Usage ./rs_ec <filename> <n> <m> <file|null>" << std::endl
			<< "\tfilename - name of the input file to be encoded" << std::endl
			<< "\tn - code block size " << std::endl
			<< "\tm - number of parities" << std::endl
			<< "\tfile| null - write to files or /dev/null to reduce disk bottleneck" << std::endl;
		return 1;
	}

	int fd;
*/
	

	return 0;
}
